# 評価基準の計算方法

このドキュメントでは、月次評価システムで使用される各評価指標の計算方法を詳しく解説します。

## 評価指標一覧

月次評価システムでは、以下の5つの指標を基に総合スコアを算出します：

1. **遅刻回数** (lateCount)
2. **申請遵守率** (applicationComplianceRate)
3. **打刻修正回数** (fixRequestCount)
4. **連続勤務日数** (consecutiveWorkDays)
5. **残業申請の正確性** (overtimeAccuracy)

---

## 1. 遅刻回数 (lateCount)

### 計算方法

```
遅刻回数 = その月の出勤打刻時刻が始業時間を超えた回数
```

### 詳細

- **対象データ**: 指定月の全勤怠記録（Attendance）
- **判定基準**: 
  - 従業員の設定された始業時間（`User.startTime`）を基準とする
  - 出勤打刻時刻（`Attendance.checkIn`）が始業時間より遅い場合、1回としてカウント
  - 始業時間が設定されていない場合は、遅刻回数 = 0

### 計算ロジック

```java
for (各勤怠記録) {
    if (出勤打刻時刻 != null && 出勤打刻時刻 > 始業時間) {
        遅刻回数++;
    }
}
```

### 例

- 始業時間: 09:00
- 出勤打刻: 09:05 → 遅刻1回
- 出勤打刻: 08:55 → 遅刻なし
- 出勤打刻: 09:15 → 遅刻1回

**結果**: 遅刻回数 = 2回

---

## 2. 申請遵守率 (applicationComplianceRate)

### 計算方法

```
申請遵守率 = (承認された修正依頼数 / 全修正依頼数) × 100%
```

### 詳細

- **対象データ**: 指定月に作成された全修正依頼（FixRequest）
- **判定基準**:
  - 修正依頼のステータスが `APPROVED`（承認済み）のものをカウント
  - 修正依頼が1件もない場合は、100% とする（申請がない = 遵守率100%）

### 計算ロジック

```java
if (修正依頼数 == 0) {
    申請遵守率 = 100.0%
} else {
    承認済み数 = ステータスが"APPROVED"の修正依頼数
    申請遵守率 = (承認済み数 / 全修正依頼数) × 100.0
}
```

### 例

- 修正依頼数: 5件
- 承認済み: 4件
- 却下: 1件

**結果**: 申請遵守率 = (4 / 5) × 100 = 80.0%

---

## 3. 打刻修正回数 (fixRequestCount)

### 計算方法

```
打刻修正回数 = 指定月に作成された修正依頼の総数
```

### 詳細

- **対象データ**: 指定月に作成された全修正依頼（FixRequest）
- **判定基準**:
  - 修正依頼の作成日時（`FixRequest.createdAt`）が指定月の範囲内にあるものをカウント
  - 承認・却下の状態に関係なく、作成された修正依頼を全てカウント

### 計算ロジック

```java
打刻修正回数 = 指定月の期間内に作成された修正依頼の件数
```

### 例

- 1月に作成された修正依頼: 3件
- 2月に作成された修正依頼: 2件

**1月の結果**: 打刻修正回数 = 3回

---

## 4. 連続勤務日数 (consecutiveWorkDays)

### 計算方法

```
連続勤務日数 = 指定月内で最も長い連続出勤日数
```

### 詳細

- **対象データ**: 指定月の全勤怠記録（Attendance）
- **判定基準**:
  - 出勤打刻（`Attendance.checkIn`）が記録されている日を出勤日とする
  - 月末から逆算して、連続する出勤日数を計算
  - 月内で最も長い連続出勤日数を採用

### 計算ロジック

```java
1. 月内の出勤日を日付順にソート
2. 月末から逆算して連続日数を計算
3. 最も長い連続日数を返す
```

### 例

**1月の出勤日**: 1日, 2日, 3日, 5日, 6日, 7日, 8日, 10日, 11日, 12日

- 連続1: 1日〜3日 → 3日間
- 連続2: 5日〜8日 → 4日間
- 連続3: 10日〜12日 → 3日間

**結果**: 連続勤務日数 = 4日（最長の連続期間）

---

## 5. 残業申請の正確性 (overtimeAccuracy)

### 計算方法

```
残業申請の正確性 = (残業が適切に記録された日数 / 総勤務日数) × 100%
```

### 詳細

- **対象データ**: 指定月の全勤怠記録（Attendance）
- **判定基準**:
  - 実働時間 = 退勤時刻 - 出勤時刻 - 休憩時間 - 中抜け時間（控除）
  - 実働時間が8時間を超える日を「残業日」とする
  - 残業日で、退勤打刻が記録されている場合を「適切に記録された」とみなす
  - 勤務記録がない場合は、100% とする

### 計算ロジック

```java
for (各勤怠記録) {
    if (出勤打刻 != null && 退勤打刻 != null) {
        総勤務日数++;
        
        実働時間 = (退勤時刻 - 出勤時刻) - 休憩時間 - 中抜け時間
        if (実働時間 > 8時間) {
            if (退勤打刻が記録されている) {
                適切に記録された残業日数++;
            }
        }
    }
}

if (総勤務日数 == 0) {
    残業申請の正確性 = 100.0%
} else {
    残業申請の正確性 = (適切に記録された残業日数 / 総勤務日数) × 100.0
}
```

### 実働時間の計算

```
実働時間 = (退勤時刻 - 出勤時刻) - 休憩時間 - 中抜け時間（控除）

休憩時間 = BreakRecord の合計時間
中抜け時間 = LeaveRecord の控除対象時間（有給として扱われない分）
```

### 例

**1月の勤務記録**:
- 1月1日: 実働8.5時間（残業0.5時間）→ 退勤打刻あり → 適切
- 1月2日: 実働7.5時間（残業なし）
- 1月3日: 実働9.0時間（残業1.0時間）→ 退勤打刻あり → 適切
- 1月4日: 実働8.2時間（残業0.2時間）→ 退勤打刻なし → 不適切

- 総勤務日数: 4日
- 残業日数: 3日（1日, 3日, 4日）
- 適切に記録された残業日数: 2日（1日, 3日）

**結果**: 残業申請の正確性 = (2 / 4) × 100 = 50.0%

---

## 総合スコア (totalScore)

### 計算方法

各指標を100点満点で正規化し、等重みで平均を算出します。

```
総合スコア = (遅刻スコア + 申請遵守率 + 打刻修正スコア + 連続勤務スコア + 残業申請の正確性) / 5
```

### 各指標のスコア化

#### 1. 遅刻スコア (lateScore)
```
遅刻スコア = max(0, 100 - 遅刻回数 × 10)
```
- 0回 = 100点
- 1回 = 90点
- 2回 = 80点
- 3回 = 70点
- ...（10点ずつ減点）

#### 2. 申請遵守率 (complianceScore)
```
申請遵守率 = そのまま使用（既に%で表現されている）
```
- 100% = 100点
- 80% = 80点

#### 3. 打刻修正スコア (fixScore)
```
打刻修正スコア = max(0, 100 - 打刻修正回数 × 5)
```
- 0回 = 100点
- 1回 = 95点
- 2回 = 90点
- 3回 = 85点
- ...（5点ずつ減点）

#### 4. 連続勤務スコア (consecutiveScore)
```
連続勤務スコア = min(20, 連続勤務日数 × 2)
```
- 1日 = 2点
- 5日 = 10点
- 10日 = 20点（最大）
- 15日 = 20点（最大）

#### 5. 残業申請の正確性 (overtimeScore)
```
残業申請の正確性 = そのまま使用（既に%で表現されている）
```
- 100% = 100点
- 80% = 80点

### 計算例

**評価データ**:
- 遅刻回数: 2回 → 遅刻スコア = 100 - 2×10 = 80点
- 申請遵守率: 85.0% → 申請遵守率スコア = 85.0点
- 打刻修正回数: 3回 → 打刻修正スコア = 100 - 3×5 = 85点
- 連続勤務日数: 10日 → 連続勤務スコア = min(20, 10×2) = 20点
- 残業申請の正確性: 92.0% → 残業申請の正確性スコア = 92.0点

**総合スコア**:
```
総合スコア = (80 + 85.0 + 85 + 20 + 92.0) / 5
          = 362.0 / 5
          = 72.4点
```

### 評価ランク

総合スコアに基づいて、以下の評価ランクが自動的に決定されます：

- **S**: 90点以上
- **A**: 80点以上90点未満
- **B**: 70点以上80点未満
- **C**: 60点以上70点未満
- **D**: 60点未満

---

## 計算タイミング

評価は以下のタイミングで自動計算・更新されます：

1. **毎月1日 午前2時**: 前月の評価を自動計算・保存
2. **毎日 午前3時**: 当月の評価を再計算（データ更新に対応）
3. **評価ダッシュボードアクセス時**: 必要に応じて評価を計算

---

## 注意事項

1. **始業時間の設定**: 遅刻回数の計算には、従業員の始業時間設定が必要です。設定されていない場合は遅刻回数 = 0 となります。

2. **休憩時間と中抜け時間**: 残業申請の正確性の計算では、休憩時間（BreakRecord）と中抜け時間（LeaveRecord）を正確に考慮します。

3. **連続勤務日数の計算**: 月末から逆算して連続日数を計算するため、月末に近い連続出勤が優先されます。

4. **申請遵守率**: 修正依頼が1件もない場合は、100% として評価されます（申請がない = 遵守率100%）。

5. **残業申請の正確性**: 現在の実装では、残業日で退勤打刻が記録されていれば「適切」とみなします。将来的には、残業申請の有無も考慮する可能性があります。

