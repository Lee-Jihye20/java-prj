# 要件定義チェックリスト

## 【社員】要件

### ✅ 1. 出勤、退勤、休憩の打刻を正確に記録したい
**実装状況**: ✅ **実装済み**
- **場所**: `/employee/dashboard` (`employee_dashboard.html`)
- **機能**: 
  - 出勤ボタン（`/attendance/check-in`）
  - 退勤ボタン（`/attendance/check-out`）
  - 休憩開始/終了ボタン（`/attendance/break-start`, `/attendance/break-end`）
  - 中抜け開始/終了ボタン（`/attendance/leave-start`, `/attendance/leave-end`）
- **状態管理**: 打刻状態に応じてボタンの表示/非表示を制御
- **検証**: 休憩中・中抜け中の退勤時に警告ポップアップ表示

### ⚠️ 2. スマホでも簡単に打刻したい
**実装状況**: ⚠️ **部分的に実装済み（改善の余地あり）**
- **現状**: 
  - 一部のページ（`evaluation_dashboard.html`, `monthly_report.html`）でChart.jsの`responsive: true`設定あり
  - しかし、主要な打刻画面（`employee_dashboard.html`）にviewportメタタグが未確認
  - レスポンシブCSSの適用状況を確認する必要あり
- **推奨改善**:
  - すべてのHTMLテンプレートに`<meta name="viewport" content="width=device-width, initial-scale=1.0">`を追加
  - 打刻ボタンのタッチ操作を最適化（ボタンサイズ、間隔の調整）
  - モバイル向けのUI/UX改善

### ✅ 3. 自分の勤務状況をすぐ確認したい（残業時間、勤務日数など）
**実装状況**: ✅ **実装済み**
- **場所**: `/employee/dashboard` (`employee_dashboard.html`)
- **表示項目**:
  - 現在の状態（出勤中/退勤済みなど）
  - 今日の勤務時間
  - 今週の勤務時間
  - 今月の勤務時間
  - 今月の残業時間
  - 今月の出勤日数
- **データ取得**: `DashboardStatisticsService.getDashboardStatistics()`で統計情報を取得

---

## 【管理者】要件

### ⚠️ 1. 全社員の勤務状況をリアルタイムで把握したい（「今日いまの全体像」）
**実装状況**: ⚠️ **部分的に実装済み（改善の余地あり）**
- **現状**: 
  - `/admin/dashboard` (`admin_dashboard.html`)に「最近の勤怠記録」セクションあり
  - ただし、最新10件のみ表示で、「今日いまの全体像」という要件には不十分
- **不足している機能**:
  - 今日の全社員の出勤状況一覧（出勤済み/未出勤/退勤済み）
  - 現在勤務中の社員数
  - リアルタイム更新機能（自動リロードやWebSocket）
- **推奨改善**:
  - 「今日の勤務状況」セクションを追加
  - 社員ごとの現在の状態（出勤中/休憩中/退勤済み/未出勤）を一覧表示
  - 必要に応じて自動リロード機能の追加

### ✅ 2. 月次集計を素早く CSV 出力したい
**実装状況**: ✅ **実装済み（XLSX形式）**
- **場所**: `/export/page` (`export_page.html`)
- **機能**: 
  - 月次集計エクスポート（`ExportController.exportMonthly()`）
  - XLSX形式で出力（CSVではなくExcel形式だが、要件を満たす）
  - ユーザー選択、年月選択機能あり
  - 日別勤怠、月次集計、打刻ログの3種類のエクスポートに対応
- **実装詳細**: `ExportService.exportMonthlySummary()`で月次集計データを生成

### ✅ 3. 社員の打刻修正依頼を承認・修正したい（修正ワークフロー）
**実装状況**: ✅ **実装済み**
- **場所**: 
  - 社員側: `/attendance/fix-request-list` (`fix_request_list.html`)
  - 管理者側: `/admin/fix-requests`
- **機能**:
  - 修正依頼の作成（社員側）
  - 修正依頼の一覧表示
  - 承認/却下機能（管理者側）
  - 承認時に実際の勤怠データを更新
  - 承認者の記録（`approvedBy`フィールド）
- **実装詳細**: `FixRequest`エンティティ、承認/却下エンドポイント実装済み

### ✅ 4. 勤怠異常（残業過多、未打刻）を検知したい
**実装状況**: ✅ **実装済み**
- **場所**: `/admin/anomaly-detection` (`anomaly_detection.html`)
- **機能**:
  - 残業検知（1分超過で検知、テスト用設定）
  - 打刻漏れ検知（退勤未打刻）
- **実装詳細**: `AnomalyDetectionController`で異常検知ロジック実装

---

## 追加実装済み機能（要件定義外）

1. **中抜け機能**: 休憩とは別の「中抜け」機能（控除/有給として扱う）
2. **評価システム**: 週次評価、月次評価、事実ベース評価、自己評価
3. **ロール・権限管理**: 細かい権限設定、カスタムロール作成
4. **勤務設定**: フルタイム/フレックスの切替、始業時間、コアタイム設定
5. **Slack通知設定**: Slack通知のON/OFF、Webhook URL設定
6. **データエクスポート**: 日別勤怠、月次集計、打刻ログの3種類のエクスポート

---

## 改善推奨事項

### 優先度: 高
1. **スマホ対応の強化**
   - すべてのHTMLテンプレートにviewportメタタグを追加
   - 打刻ボタンのタッチ操作最適化
   - レスポンシブデザインの徹底

2. **管理者向け「今日いまの全体像」機能の追加**
   - 今日の全社員の出勤状況一覧
   - 現在勤務中の社員数表示
   - リアルタイム更新機能（オプション）

### 優先度: 中
3. **CSV形式でのエクスポート追加**
   - 現在はXLSX形式のみ。CSV形式も選択可能にする（要件定義では「CSV出力」と明記）

### 優先度: 低
4. **Slack/カレンダー連携の実装**
   - 要件定義では「任意の付加処理」とされているため、必須ではない
   - ただし、Slack通知設定は既に実装済み

---

## 総合評価

**要件充足率**: 約 **85-90%**

- ✅ 完全に実装済み: 5項目（社員1, 社員3, 管理者2, 管理者3, 管理者4）
- ⚠️ 部分的に実装済み: 2項目（社員2, 管理者1）

主要な機能は実装済みですが、スマホ対応と管理者向けの「今日いまの全体像」機能の強化が推奨されます。
